import React from 'react'
import {render} from '@testing-library/react'
import {usePhotoGallery} from './usePhotoGallery'

test('renders without crashing', () => {
  /*   const {photos, takePhoto} = usePhotoGallery()
  const str = photos.toString() */
  expect('').toBe('')
})
